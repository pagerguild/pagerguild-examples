# PagerGuild with MySQL

## Configuration

In the PagerGuild configuration file, [`pagerguild.yaml`](pagerguild.yml), add a `databaseType` field, with value indicating the MySQL version of your production database.
